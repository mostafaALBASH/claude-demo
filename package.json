{
  "name": "taskflow",
  "version": "1.0.0",
  "description": "A lightweight task manager API — demo project for Claude Code in Action",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "test": "node --test tests/*.test.js",
    "lint": "npx eslint src/",
    "format": "npx prettier --write 'src/**/*.js'"
  },
  "keywords": ["tasks", "api", "express", "claude-code"],
  "author": "Your Name",
  "license": "MIT",
  "dependencies": {
    "better-sqlite3": "^11.7.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.1",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "eslint": "^9.17.0",
    "prettier": "^3.4.2"
  }
}
